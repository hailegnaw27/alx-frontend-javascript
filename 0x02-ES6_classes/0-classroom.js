// Define a class named ClassRoom
export default class ClassRoom {
  // Constructor to initialize the class with a maxStudentsSize parameter
  constructor(maxStudentsSize) {
    // Assign the maxStudentsSize parameter to the _maxStudentsSize property
    this._maxStudentsSize = maxStudentsSize;
  }
}
